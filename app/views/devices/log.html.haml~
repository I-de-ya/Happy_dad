%ul
	- @device.logs.each do |log|
		%li
			Значение атрибута
			%b= log.attr
			- if log.old_value == nil
				было установлено равным					
				%b= log.new_value
			- else
				было изменено с 		
				%b= log.old_value
				на
				%b= log.new_value
		= log.created_at.to_s(:db)
		%hr
